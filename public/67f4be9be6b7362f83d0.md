---
title: 【備忘録】Google Apps Scriptで作る！カレンダーイベント一括登録ツール
tags:
  - GoogleCalendar
  - GAS
private: false
updated_at: '2025-07-30T12:28:33+09:00'
id: 67f4be9be6b7362f83d0
organization_url_name: null
slide: false
ignorePublish: false
---
# Google Apps Scriptで作る！カレンダーイベント一括登録ツール

    在宅の予定　とか、一括で登録するアプリ作った。GASで。

    こんな感じ


![スクリーンショット 2025-07-20 103225.jpg](https://qiita-image-store.s3.ap-northeast-1.amazonaws.com/0/2433285/f3ad5bf1-afab-4905-8c98-b249e5ab04bf.jpeg)

備忘録


## はじめに

Googleカレンダーにイベントを登録する際、同じイベントを複数の日付に一個ずつ登録するのは面倒ではありませんか？

この記事では、**Google Apps Script（GAS）を使って複数日付に一括でイベントを登録できるWebアプリ**を作成する方法をご紹介します。

## このツールでできること

### 🎯 主な機能

- **複数日付への一括イベント登録** - 選択した複数の日付に同じイベントを一度に追加
- **終日・時間指定の両方に対応** - 勤務日や会議など用途に応じて選択可能
- **よく使うイベント名の保存・再利用** - 定期的なイベントを効率的に管理
- **イベント色の設定** - カレンダー上で視覚的に分類
- **直感的なカレンダーUI** - 月表示で日付を選択

### 💡 こんな場面で便利

- 毎月の勤務日を一括登録
- 定期会議やイベントの予定追加
- 休暇や出張の予定管理
- チームメンバーとの予定共有

## 実装コード

### Code.gs（Google Apps Script側）

```javascript
/**
 * ウェブアプリケーションのHTMLファイルを提供します。
 * この関数は、ユーザーがアプリにアクセスした際に自動的に呼び出されます。
 * @returns {GoogleAppsScript.HTML.HtmlOutput} HTMLファイルの内容
 */
function doGet() {
  // 'Index' HTMLファイルの名前で作成 (例: Index.html)
  return HtmlService.createTemplateFromFile('Index').evaluate()
    .setXFrameOptionsMode(HtmlService.XFrameOptionsMode.ALLOWALL);
}

/**
 * 現在のウェブアプリユーザーのメール、イベントリスト。
 * ユーザー情報を取得していないイベント名リストを返します。
 * @returns {Object} ユーザーのメールアドレスとイベント名リストを含むオブジェクト
 */
function getCurrentUserEmailAndEventNames() {
  try {
    var userEmail = Session.getActiveUser().getEmail();
    var eventNames = []; // { name: "例1", isAllDay: true, startTime: "", endTime: "", eventColor: "" }]
    
    if (userEmail) {
      var userProperties = PropertiesService.getUserProperties();
      var storedNamesJson = userProperties.getProperty('savedEventNames');
      if (storedNamesJson) {
        eventNames = JSON.parse(storedNamesJson);
      }
    }
    
    return { userEmail: userEmail, eventNames: eventNames };
  } catch (e) {
    Logger.log('ユーザー情報とイベントリストの取得中にエラーが発生しました: ' + e.message);
    // エラーが発生した場合でも、部分的に情報を返す
    return { userEmail: Session.getActiveUser().getEmail() || "", eventNames: [] };
  }
}

/**
 * ユーザーのイベントリストに新しいイベント名を追加します。
 * @param {Object} newEventDetails 追加するイベントの詳細 { name: string, isAllDay: boolean, startTime: string, endTime: string, eventColor: string }
 * @returns {Object[]} 更新されたイベント名リスト
 */
function addEventNames(newEventDetails) {
  var userEmail = Session.getActiveUser().getEmail();
  if (!userEmail) {
    throw new Error('ユーザーが認証されていません。');
  }
  
  var userProperties = PropertiesService.getUserProperties();
  var storedNamesJson = userProperties.getProperty('savedEventNames');
  var eventNames = [];
  
  if (storedNamesJson) {
    eventNames = JSON.parse(storedNamesJson);
  }
  
  // 重複チェック（名前のみの場合をとりあえず確認）
  var nameExists = eventNames.some(function(item) {
    return item.name === newEventDetails.name;
  });
  
  if (!nameExists) {
    eventNames.push(newEventDetails);
    userProperties.setProperty('savedEventNames', JSON.stringify(eventNames));
  }
  
  return eventNames;
}

/**
 * ユーザーのイベントリストから指定されたイベントを検索します。
 * @param {string} nameToDelete 削除するイベント名（名前で確認）
 * @returns {Object[]} 更新されたイベント名リスト
 */
function deleteEventNameFromList(nameToDelete) {
  var userEmail = Session.getActiveUser().getEmail();
  if (!userEmail) {
    throw new Error('ユーザーが認証されていません。');
  }
  
  var userProperties = PropertiesService.getUserProperties();
  var storedNamesJson = userProperties.getProperty('savedEventNames');
  var currentNames = [];
  
  if (storedNamesJson) {
    currentNames = JSON.parse(storedNamesJson);
  }
  
  var updatedNames = currentNames.filter(function(item) {
    return item.name !== nameToDelete; // 名前でフィルタリング
  });
  
  userProperties.setProperty('savedEventNames', JSON.stringify(updatedNames));
  
  return updatedNames;
}

/**
 * ユーザーのイベントリストから該当するイベントを検索します。
 * @param {string} eventName 取得するイベント名
 * @returns {Object|null} 更新されたイベント詳細またはnull
 */
function getEventNameDetails(eventName) {
  var userEmail = Session.getActiveUser().getEmail();
  if (!userEmail) {
    throw new Error('ユーザーが認証されていません。');
  }
  
  var userProperties = PropertiesService.getUserProperties();
  var storedNamesJson = userProperties.getProperty('savedEventNames');
  
  if (storedNamesJson) {
    var currentNames = JSON.parse(storedNamesJson);
    var existingEvent = currentNames.find(function(item) {
      return item.name === eventName;
    });
    return existingEvent || null;
  }
  return null;
}

/**
 * ウェブアプリケーションの日付取得を行われ、Googleカレンダーにイベントを追加します。
 * @param {string} calendarId イベントを追加するカレンダーのID（メールアドレス形式）
 * @param {string} month イベントを追加する月（YYYY-MM形式）
 * @param {string} days イベントを追加する日付の配列（例: ['1', '5', '10']）
 * @param {string} eventTitle イベントのタイトル
 * @param {boolean} isAllDay 終日イベントかどうか
 * @param {string} startTimeStr 開始時間の文字列（HH:MM形式）
 * @param {string} endTimeStr 終了時間の文字列（HH:MM形式）
 * @param {string} eventColor イベントの色（例: 'BLUE', 'RED'）
 * @returns {string} 処理結果のメッセージ
 */
function addEventsToCalendarDirectly(calendarId, month, days, eventTitle, isAllDay, startTimeStr, endTimeStr, eventColor) {
  try {
    var calendar = CalendarApp.getCalendarById(calendarId);
    if (!calendar) {
      throw new Error('指定されたカレンダーID発見ができません: ' + calendarId + '。正しいメールアドレスを入力してください。');
    }
    
    var addedCount = 0;
    days.forEach(function(day) {
      // YYYY-MM-DD 形式の日付文字列を作成
      var dateString = month + '-' + String(day).padStart(2, '0');
      
      var baseDate = new Date(dateString); // 日付分の分
      
      var eventStart;
      var eventEnd;
      
      if (isAllDay) {
        // 終日イベントの場合
        eventStart = new Date(baseDate);
        eventStart.setHours(0, 0, 0, 0); // その日の00時00分00秒に設定
        eventEnd = new Date(baseDate);
        eventEnd.setHours(23, 59, 59, 999); // その日の23時59分59秒999ミリ秒に設定
      } else {
        // 時間指定イベントの場合
        var startTimeParts = startTimeStr.split(':');
        var endTimeParts = endTimeStr.split(':');
        var startHour = parseInt(startTimeParts[0]);
        var startMinute = parseInt(startTimeParts[1]);
        var endHour = parseInt(endTimeParts[0]);
        var endMinute = parseInt(endTimeParts[1]);
        
        eventStart = new Date(baseDate);
        eventStart.setHours(startHour, startMinute, 0, 0);
        
        eventEnd = new Date(baseDate);
        eventEnd.setHours(endHour, endMinute, 0, 0);
      }
      
      // 終了時間が開始時間より前の場合、翌日に設定する（例: 23:00 - 01:00）
      if (eventEnd.getTime() < eventStart.getTime()) {
        eventEnd.setDate(eventEnd.getDate() + 1);
      }
      
      // 既存のイベントを検索（部分一致）
      var searchStartTime = new Date(baseDate);
      searchStartTime.setHours(0, 0, 0, 0);
      var searchEndTime = new Date(baseDate);
      searchEndTime.setHours(23, 59, 59, 999);
      var events = calendar.getEvents(searchStartTime, searchEndTime, { search: eventTitle });
      
      // 完全一致するタイトルのイベントがあるか確認
      var eventExists = false;
      events.forEach(function(existingEvent) {
        if (existingEvent.getTitle() === eventTitle) {
          eventExists = true;
        }
      });
      
      // イベントが存在しないリスト仕込み
      if (!eventExists) {
        var newEvent;
        if (isAllDay) {
          newEvent = calendar.createAllDayEvent(eventTitle, baseDate);
        } else {
          newEvent = calendar.createEvent(eventTitle, eventStart, eventEnd);
        }
        
        // イベントの色を設定（修正版：対応色のみ処理）
        Logger.log('Received eventColor for setting: ' + eventColor);
        if (eventColor && isValidEventColor(eventColor)) {
          try {
            newEvent.setColor(CalendarApp.EventColor[eventColor]);
            Logger.log('Color set successfully to: ' + eventColor);
          } catch (colorError) {
            Logger.log('Color setting failed: ' + colorError.message + ', eventColor: ' + eventColor);
            // 色設定に失敗してもイベントは作成する
          }
        } else {
          Logger.log('Invalid or empty eventColor: "' + eventColor + '"');
        }
        
        addedCount++;
      }
    });
    
    return addedCount + '件のイベントをカレンダーに追加しました。';
  } catch (e) {
    Logger.log('イベント追加中にエラーが発生しました: ' + e.message);
    throw new Error('イベントの追加中にエラーが発生しました: ' + e.message);
  }
}

/**
 * 指定された色がGoogleカレンダーでサポートされているかチェックします。
 * レポートで定義された有効な色のみを許可します。
 * @param {string} eventColor 確認する色名
 * @returns {boolean} 有効な色かどうか
 */
function isValidEventColor(eventColor) {
  // レポートで定義された有効な色リスト（BROWN, LIGHT_BLUEなどの未対応色を除外）
  var validColors = [
    'LAVENDER',  // ID: 1, ラベンダー
    'SAGE',      // ID: 2, セージ
    'PURPLE',    // ID: 3, ブドウ(紫)
    'PINK',      // ID: 4, フラミンゴ(ピンク)
    'YELLOW',    // ID: 5, バナナ(黄)
    'ORANGE',    // ID: 6, ミカン(オレンジ)
    'CYAN',      // ID: 7, ピーコック(水色)
    'GREY',      // ID: 8, グラファイト(グレー)
    'BLUE',      // ID: 9, ブルーベリー(青)
    'GREEN',     // ID: 10, バジル(緑)
    'RED'        // ID: 11, トマト(赤)
  ];
  
  return validColors.indexOf(eventColor) !== -1;
}
```

### Index.html（フロントエンド）- スマホ対応完全版

```html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>GASカレンダーイベント登録ツール</title>
  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      font-family: "Inter", sans-serif;
      background-color: #f0f4f8; /* Light blue-gray background */
    }
    
    .day-grid button {
      transition: background-color 0.2s, transform 0.1s;
    }
    
    .day-grid button.selected {
      background-color: #3b82f6; /* Blue-600 */
      color: white;
    }
    
    .day-grid button:hover:not(.selected) {
      background-color: #e0e7ff; /* Indigo-100 */
      transform: translateY(-1px);
    }
    
    .day-grid button:active {
      transform: translateY(0);
    }
    
    .message-success {
      background-color: #d1fae5; /* Green-100 */
      color: #065f46; /* Green-800 */
    }
    
    .message-error {
      background-color: #fee2e2; /* Red-100 */
      color: #991b1b; /* Red-800 */
    }
    
    .event-name-item {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0.5rem 0.75rem;
      background-color: #f9fafb; /* Gray-100 */
      border-radius: 0.5rem;
      margin-bottom: 0.5rem;
      font-size: 0.9rem;
    }
    
    .event-name-item button {
      background-color: #ef4444; /* Red-500 */
      color: white;
    }
    
    .event-name-item button:hover {
      background-color: #dc2626; /* Red-600 */
      transition: background-color 0.2s;
    }
    
    /* スマホでの文字サイズ調整 */
    @media (max-width: 640px) {
      .mobile-friendly-text {
        font-size: 18px !important;
      }
      
      .mobile-friendly-button {
        font-size: 20px !important;
        padding: 18px !important;
        min-height: 56px;
      }
      
      .mobile-friendly-input {
        font-size: 18px !important;
        padding: 18px !important;
        min-height: 56px;
      }
      
      .mobile-friendly-select {
        font-size: 18px !important;
        padding: 18px !important;
        min-height: 56px;
      }
      
      .day-grid button {
        font-size: 18px !important;
        padding: 16px !important;
        min-height: 56px;
        min-width: 56px;
      }
      
      .mobile-friendly-label {
        font-size: 18px !important;
        font-weight: 600;
        margin-bottom: 12px !important;
      }
      
      .event-name-item {
        padding: 16px !important;
        font-size: 18px !important;
        margin-bottom: 12px !important;
      }
      
      .event-name-item button {
        font-size: 16px !important;
        padding: 12px 16px !important;
        min-height: 48px;
      }
      
      input[type="radio"] {
        width: 24px !important;
        height: 24px !important;
        margin-right: 12px !important;
      }
      
      /* タイトルも大きく */
      h1 {
        font-size: 28px !important;
        margin-bottom: 24px !important;
      }
      
      h3 {
        font-size: 22px !important;
      }
      
      /* 全体のスペースも調整 */
      .space-y-6 > * + * {
        margin-top: 32px !important;
      }
    }
  </style>
</head>
<body class="p-4 sm:p-8 md:p-12 lg:p-16 flex justify-center items-start min-h-screen">
  <div class="bg-white p-6 sm:p-8 rounded-xl shadow-lg w-full max-w-4xl border border-gray-200">
    <h1 class="text-2xl sm:text-3xl font-bold text-gray-800 mb-5 text-center mobile-friendly-label">GASカレンダーイベント登録ツール</h1>
    
    <div class="space-y-6">
      <!-- カレンダーID入力 -->
      <div>
        <label for="calendarId" class="block text-gray-700 text-sm font-medium mb-2 mobile-friendly-label">カレンダーID:</label>
        <input type="email" id="calendarId" placeholder="例: your_email@example.com"
               class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent text-gray-800 mobile-friendly-input"
               value=""> <!-- 初期値を空に設定 -->
      </div>
      
      <!-- 月選択 -->
      <div>
        <label for="month" class="block text-gray-700 text-sm font-medium mb-2 mobile-friendly-label">月:</label>
        <input type="month" id="month" 
               class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent text-gray-800 mobile-friendly-input">
      </div>
      
      <!-- 日付選択 -->
      <div>
        <label class="block text-gray-700 text-sm font-medium mb-2 mobile-friendly-label">日付選択:</label>
        <div id="dayGrid" class="day-grid grid grid-cols-7 gap-2 max-h-64 overflow-y-auto pr-2 mt-3">
          <!-- 動的に生成される日付ボタンがここに表示されます -->
        </div>
        <button id="selectAllDays" class="mt-3 px-4 py-2 bg-gray-200 text-gray-700 rounded-lg hover:bg-gray-300 focus:outline-none focus:ring-2 focus:ring-gray-400 mobile-friendly-button">
          全日付選択/取消
        </button>
      </div>
      
      <!-- イベント名選択 -->
      <div>
        <label for="eventTitle" class="block text-gray-700 text-sm font-medium mb-2 mobile-friendly-label">イベント名:</label>
        <input type="text" id="eventTitle" placeholder="例: 会議"
               class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent text-gray-800 mobile-friendly-input"
               value=""> <!-- 初期値を空に設定 -->
      </div>
      
      <!-- イベントタイプ選択（終日/時間指定） -->
      <div>
        <label class="block text-gray-700 text-sm font-medium mb-2 mobile-friendly-label">イベントタイプ:</label>
        <div class="flex items-center space-x-4">
          <label class="inline-flex items-center">
            <input type="radio" name="eventType" value="allDay" id="eventTypeAllDay" class="form-radio text-blue-600" checked>
            <span class="ml-2 text-gray-800 mobile-friendly-text">終日</span>
          </label>
          <label class="inline-flex items-center">
            <input type="radio" name="eventType" value="timed" id="eventTypeTimed" class="form-radio text-blue-600">
            <span class="ml-2 text-gray-800 mobile-friendly-text">時間指定</span>
          </label>
        </div>
      </div>
      
      <!-- 時間指定入力フィールド（初期状態は非表示） -->
      <div id="timeInputs" class="grid grid-cols-1 sm:grid-cols-2 gap-4 mt-2" style="display: none;">
        <div>
          <label for="startTime" class="block text-gray-700 text-sm font-medium mb-2 mobile-friendly-label">開始時間（リスト選択）:</label>
          <input type="time" id="startTime" value="09:00"
                 class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent text-gray-800 mobile-friendly-input">
        </div>
        <div>
          <label for="endTime" class="block text-gray-700 text-sm font-medium mb-2 mobile-friendly-label">終了時間（リスト選択）:</label>
          <input type="time" id="endTime" value="17:00"
                 class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent text-gray-800 mobile-friendly-input">
        </div>
      </div>
      
      <!-- イベント色選択（修正版） -->
      <div>
        <label for="eventColor" class="block text-gray-700 text-sm font-medium mb-2 mobile-friendly-label">イベントの色（リスト選択）:</label>
        <select id="eventColor" class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent text-gray-800 mobile-friendly-select">
          <option value="">-- デフォルト --</option>
          <option value="LAVENDER">ラベンダー(LAVENDER)</option>
          <option value="SAGE">セージ(SAGE)</option>
          <option value="PURPLE">ブドウ(PURPLE)</option>
          <option value="PINK">フラミンゴ(PINK)</option>
          <option value="YELLOW">バナナ(YELLOW)</option>
          <option value="ORANGE">ミカン(ORANGE)</option>
          <option value="CYAN">ピーコック(CYAN)</option>
          <option value="GREY">グラファイト(GREY)</option>
          <option value="BLUE">ブルーベリー(BLUE)</option>
          <option value="GREEN">バジル(GREEN)</option>
          <option value="RED">トマト(RED)</option>
        </select>
      </div>
      
      <!-- 保存済み・削除（読み上げ/上書き） -->
      <div class="flex flex-col sm:flex-row items-center space-y-3 sm:space-y-0 sm:space-x-4">
        <button id="addEventButton"
               class="w-full bg-blue-600 text-white p-3 rounded-lg font-semibold hover:bg-blue-700 focus:outline-none focus:ring-4 focus:ring-blue-300 shadow-md mobile-friendly-button">
          カレンダーにイベント登録
        </button>
        <button id="clearInputs"
               class="w-full bg-red-500 text-white p-3 rounded-lg font-semibold hover:bg-red-600 focus:outline-none focus:ring-4 focus:ring-red-300 shadow-md mobile-friendly-button">
          クリア
        </button>
      </div>
      
      <!-- メッセージ表示エリア（成功・エラー用など） -->
      <div id="messageArea" class="text-sm text-center font-medium hidden mobile-friendly-text"></div>
      
      <!-- 個人用イベントリスト -->
      <div>
        <h3 class="text-lg font-semibold text-gray-800 mb-2 mobile-friendly-label">イベントリスト:</h3>
        <div class="text-sm text-gray-600 mb-2 mobile-friendly-text">保存済み名前を選択すると、イベント名、イベントタイプ、時間、色が自動で設定されます。</div>
        <div class="border border-gray-300 rounded-lg p-3 min-h-20">
          <div id="eventNamesList" class="text-sm text-gray-600 mb-2 mobile-friendly-text">保存されたイベントがここに表示されます。</div>
        </div>
      </div>
      
      <!-- 既存のイベント名選択 -->
      <div class="border border-gray-300 rounded-lg p-4 bg-gray-50 mt-6">
        <label for="selectSavedEventName" class="block text-gray-700 text-sm font-medium mb-2 mobile-friendly-label">保存済みイベント名:</label>
        <div class="text-sm text-gray-600 mb-2 mobile-friendly-text">保存されてる、1つのイベント名、イベントタイプ、時間、色が自動設定されます。</div>
        <select id="selectSavedEventName"
               class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent text-gray-800 mobile-friendly-select">
          <option value="">-- 選択してください --</option>
          <!-- 保存されたイベント名がここに追加されます -->
        </select>
      </div>
      
      <!-- 新しいイベント名登録 -->
      <div id="newEventNameInputs" class="grid grid-cols-1 sm:grid-cols-2 gap-4 mt-2" style="display: none;">
        <div>
          <label for="newStartTime" class="block text-gray-700 text-sm font-medium mb-2 mobile-friendly-label">開始時間（リスト選択）:</label>
          <input type="time" id="newStartTime" value="09:00"
                 class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent text-gray-800 mobile-friendly-input">
        </div>
        <div>
          <label for="newEndTime" class="block text-gray-700 text-sm font-medium mb-2 mobile-friendly-label">終了時間（リスト選択）:</label>
          <input type="time" id="newEndTime" value="17:00"
                 class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent text-gray-800 mobile-friendly-input">
        </div>
      </div>
      
      <!-- 新しいイベント名追加のイベント選択 -->
      <div class="mt-4">
        <label class="block text-gray-700 text-sm font-medium mb-2 mobile-friendly-label">イベントタイプ（保存用設定）:</label>
        <div class="flex items-center space-x-4">
          <label class="inline-flex items-center">
            <input type="radio" name="newEventEventType" value="allDay" id="newEventEventTypeAllDay"
                   class="form-radio text-blue-600" checked>
            <span class="ml-2 text-gray-800 mobile-friendly-text">終日</span>
          </label>
          <label class="inline-flex items-center">
            <input type="radio" name="newEventEventType" value="timed" id="newEventEventTypeTimed"
                   class="form-radio text-blue-600">
            <span class="ml-2 text-gray-800 mobile-friendly-text">時間指定</span>
          </label>
        </div>
      </div>
      
      <!-- 新しいイベント名入力 -->
      <div>
        <label for="newEventNameInput" class="block text-gray-700 text-sm font-medium mb-2 mobile-friendly-label">新しいイベント名:</label>
        <input type="text" id="newEventNameInput" placeholder="新しいイベント名を入力"
               class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent text-gray-800 mobile-friendly-input">
      </div>
      
      <!-- 新しいイベント名の色選択（修正版） -->
      <div>
        <label for="newEventColor" class="block text-gray-700 text-sm font-medium mb-2 mobile-friendly-label">イベントの色（保存用）:</label>
        <select id="newEventColor" class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent text-gray-800 mobile-friendly-select">
          <option value="">-- デフォルト --</option>
          <option value="LAVENDER">ラベンダー(LAVENDER)</option>
          <option value="SAGE">セージ(SAGE)</option>
          <option value="PURPLE">ブドウ(PURPLE)</option>
          <option value="PINK">フラミンゴ(PINK)</option>
          <option value="YELLOW">バナナ(YELLOW)</option>
          <option value="ORANGE">ミカン(ORANGE)</option>
          <option value="CYAN">ピーコック(CYAN)</option>
          <option value="GREY">グラファイト(GREY)</option>
          <option value="BLUE">ブルーベリー(BLUE)</option>
          <option value="GREEN">バジル(GREEN)</option>
          <option value="RED">トマト(RED)</option>
        </select>
        <button id="addEventNameButton"
               class="mt-3 px-4 py-2 bg-green-600 text-white rounded-lg font-semibold hover:bg-green-700 focus:outline-none focus:ring-4 focus:ring-green-300 mobile-friendly-button w-full sm:w-auto">
          イベント名を追加
        </button>
      </div>
    </div>
  </div>
  
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      const calendarIdInput = document.getElementById('calendarId');
      const monthInput = document.getElementById('month');
      const dayGridDiv = document.getElementById('dayGrid');
      const selectAllDaysButton = document.getElementById('selectAllDays');
      const eventTitleInput = document.getElementById('eventTitle');
      const newEventNameInput = document.getElementById('newEventNameInput');
      const addEventButton = document.getElementById('addEventButton');
      const clearInputsButton = document.getElementById('clearInputs');
      const eventTypeAllDay = document.getElementById('eventTypeAllDay');
      const eventTypeTimed = document.getElementById('eventTypeTimed');
      const timeInputsDiv = document.getElementById('timeInputs');
      const startTimeInput = document.getElementById('startTime');
      const endTimeInput = document.getElementById('endTime');
      const eventColorInput = document.getElementById('eventColor');
      const selectSavedEventName = document.getElementById('selectSavedEventName');
      const newEventEventTypeAllDay = document.getElementById('newEventEventTypeAllDay');
      const newEventEventTypeTimed = document.getElementById('newEventEventTypeTimed');
      const newStartTimeInput = document.getElementById('newStartTime');
      const newEndTimeInput = document.getElementById('newEndTime');
      const newEventColorInput = document.getElementById('newEventColor');
      const addEventNameButton = document.getElementById('addEventNameButton');
      const messageArea = document.getElementById('messageArea');
      const eventNamesListDiv = document.getElementById('eventNamesList');
      
      let selectedDays = new Set();
      let currentUserEmail = ''; // 現在のユーザーのメールアドレスを保持
      
      // 現在の年月を初期値に設定
      const today = new Date();
      const currentMonth = today.getFullYear() + '-' + String(today.getMonth() + 1).padStart(2, '0');
      monthInput.value = currentMonth;
      
      // メインのイベントタイプ選択のイベントリスナー
      eventTypeAllDay.addEventListener('change', toggleTimeInputs);
      eventTypeTimed.addEventListener('change', toggleTimeInputs);
      
      // 新しいイベント名追加用のイベントタイプ選択のイベントリスナー
      newEventEventTypeAllDay.addEventListener('change', toggleNewEventTimeInputs);
      newEventEventTypeTimed.addEventListener('change', toggleNewEventTimeInputs);
      
      // 初期設定：今日の日付を入力フィールドに表示する設定
      toggleTimeInputs();
      toggleNewEventTimeInputs(); // 新しいイベント名追加用の時間表示を制御
      
      // 月が変更された際に、日付グリッドを更新
      monthInput.addEventListener('change', updateDayGrid);
      
      // 月が変更された際に、日付グリッドを更新
      updateDayGrid();
      
      // データの取得済み。ユーザー情報取得イベントリストを取得
      google.script.run
        .withSuccessHandler(function(data) {
          currentUserEmail = data.userEmail;
          if (currentUserEmail) {
            calendarIdInput.value = currentUserEmail;
          } else {
            calendarIdInput.placeholder = '例: your_email@example.com';
            calendarIdInput.value = '';
          }
          renderEventNames(data.eventNames);
        })
        .withFailureHandler(function(error) {
          console.error('ユーザー情報とイベントリストの取得に失敗しました:', error.message);
          calendarIdInput.placeholder = '例: your_email@example.com';
          calendarIdInput.value = '';
          showMessage('ユーザー情報が取得できませんでした。', true);
        })
        .getCurrentUserEmailAndEventNames(); // GAS関数を呼ぶ
      
      // 保存済みイベント名選択のイベントリスナー
      selectSavedEventName.addEventListener('change', function() {
        const selectedOptionValue = this.value;
        if (selectedOptionValue) {
          // 保存されたイベント名を反映させるロジック
          google.script.run
            .withSuccessHandler(function(eventDetails) {
              if (eventDetails) {
                eventTitleInput.value = eventDetails.name;
                if (eventDetails.isAllDay) {
                  eventTypeAllDay.checked = true;
                } else {
                  eventTypeTimed.checked = true;
                }
                toggleTimeInputs(); // 状態を更新する制御
                startTimeInput.value = eventDetails.startTime || '09:00';
                endTimeInput.value = eventDetails.endTime || '17:00';
                eventColorInput.value = eventDetails.eventColor || '';
                showMessage('選択されたイベント名を入力欄に反映しました。', false);
              }
            })
            .withFailureHandler(function(error) {
              console.error('保存済みイベント名の取得に失敗しました:', error.message);
              showMessage('保存済みイベント名の読み込みに失敗しました。', true);
            })
            .getEventNameDetails(selectedOptionValue);
        }
      });
      
      // 全選択/解除ボタンのイベントリスナー
      selectAllDaysButton.addEventListener('click', function() {
        const allDayButtons = dayGridDiv.querySelectorAll('button');
        const allSelected = allDayButtons.length === selectedDays.size;
        
        allDayButtons.forEach(button => {
          const day = button.dataset.day;
          if (allSelected) {
            button.classList.remove('selected');
            selectedDays.delete(day);
          } else {
            button.classList.add('selected');
            selectedDays.add(day);
          }
        });
      });
      
      // メッセージ表示機能
      function showMessage(message, isError = false) {
        messageArea.textContent = message;
        messageArea.classList.remove('hidden', 'message-success', 'message-error');
        messageArea.classList.add(isError ? 'message-error' : 'message-success');
        
        setTimeout(() => {
          messageArea.classList.add('hidden');
        }, 5000);
      }
      
      // GAS側の成功時の処理用レンダラ
      function onSuccess(message) {
        showMessage(message, false);
      }
      
      // GAS側の失敗時の処理用レンダラ
      function onFailure(error) {
        showMessage('エラー: ' + error.message, true);
      }
      
      // イベント名のリストを表示する関数
      function renderEventNames(names) {
        selectSavedEventName.innerHTML = '<option value="">-- 選択してください --</option>';
        eventNamesListDiv.innerHTML = '';
        
        if (names && names.length > 0) {
          names.forEach(item => {
            // ドロップダウンオプション
            const option = document.createElement('option');
            option.value = item.name;
            option.textContent = item.name;
            selectSavedEventName.appendChild(option);
            
            // リスト表示
            const itemDiv = document.createElement('div');
            itemDiv.classList.add('event-name-item');
            const typeText = item.isAllDay ? '終日' : `${item.startTime || '00:00'}-${item.endTime || '00:00'}`;
            const colorText = item.eventColor ? `${item.eventColor.toLowerCase()}` : '';
            itemDiv.innerHTML = `
              <span class="mobile-friendly-text">${item.name} (${typeText}) ${colorText}</span>
              <button data-name="${item.name}" class="delete-btn px-2 py-1 text-xs mobile-friendly-button">削除</button>
            `;
            
            itemDiv.querySelector('button').addEventListener('click', function() {
              deleteEventNameFromList(this.dataset.name);
            });
            
            eventNamesListDiv.appendChild(itemDiv);
          });
        } else {
          eventNamesListDiv.innerHTML = '<p class="text-gray-600 text-sm">イベント名が登録されていません。</p>';
        }
      }
      
      // イベント名を追加する関数
      function addEventNameToList() {
        const newName = newEventNameInput.value.trim();
        if (newName) {
          const isAllDay = newEventEventTypeAllDay.checked;
          const startTime = newStartTimeInput.value;
          const endTime = newEndTimeInput.value;
          const eventColor = newEventColorInput.value;
          
          // 時間指定なのに時間が未入力な場合のエラーハンドリング
          if (!isAllDay && (!startTime || !endTime)) {
            showMessage('時間指定のイベントの場合は開始時間と終了時間を入力してください。', true);
            return;
          }
          
          showMessage('イベント名追加中...', false);
          google.script.run
            .withSuccessHandler(function(updatedNames) {
              renderEventNames(updatedNames);
              newEventNameInput.value = '';
              showMessage('イベント名が追加されました。', false);
            })
            .withFailureHandler(function(error) {
              showMessage('イベント名の追加に失敗しました: ' + error.message, true);
            })
            .addEventNames({
              name: newName,
              isAllDay: isAllDay,
              startTime: startTime,
              endTime: endTime,
              eventColor: eventColor
            });
        } else {
          showMessage('名前を入力してください。', true);
        }
      }
      
      // イベント名削除する関数
      function deleteEventNameFromList(nameToDelete) {
        showMessage('イベント名を削除中...', false);
        google.script.run
          .withSuccessHandler(function(updatedNames) {
            renderEventNames(updatedNames);
            showMessage('イベント名が削除されました。', false);
          })
          .withFailureHandler(function(error) {
            showMessage('イベント名の削除に失敗しました: ' + error.message, true);
          })
          .deleteEventNameFromList(nameToDelete);
      }
      
      // イベント名追加ボタンのイベントリスナー
      addEventNameButton.addEventListener('click', addEventNameToList);
      
      // カレンダーイベント登録ボタンの機能
      addEventButton.addEventListener('click', function() {
        const calendarId = calendarIdInput.value.trim();
        const month = monthInput.value;
        const eventTitle = eventTitleInput.value.trim();
        const isAllDay = eventTypeAllDay.checked;
        const startTime = startTimeInput.value;
        const endTime = endTimeInput.value;
        const eventColor = eventColorInput.value;
        
        if (!calendarId || !month || !eventTitle || selectedDays.size === 0) {
          showMessage('エラー: 全項目に値を入力し、日付も選択してください。', true);
          return;
        }
        
        if (!isAllDay && (!startTime || !endTime)) {
          showMessage('エラー: 時間指定イベントの場合は開始時間と終了時間を入力してください。', true);
          return;
        }
        
        const sortedDays = Array.from(selectedDays).sort((a, b) => Number(a) - Number(b));
        
        showMessage('カレンダーイベント登録中...', false);
        messageArea.classList.remove('hidden');
        
        google.script.run
          .withSuccessHandler(onSuccess)
          .withFailureHandler(onFailure)
          .addEventsToCalendarDirectly(calendarId, month, sortedDays, eventTitle, isAllDay, startTime, endTime, eventColor);
      });
      
      // クリアボタンの入力機能
      clearInputsButton.addEventListener('click', function() {
        // カレンダーIDは自動設定のため保護する
        calendarIdInput.value = currentUserEmail; // 現在のユーザーメールを再設定
        calendarIdInput.placeholder = '例: your_email@example.com'; // プレースホルダーも復元
        
        const today = new Date();
        const currentMonth = today.getFullYear() + '-' + String(today.getMonth() + 1).padStart(2, '0');
        monthInput.value = currentMonth;
        eventTitleInput.value = '';
        newEventNameInput.value = '';
        
        // 装飾用のラジオボタン初期値、色もファイルを設定
        newEventEventTypeAllDay.checked = true;
        toggleNewEventTimeInputs();
        newStartTimeInput.value = '09:00';
        newEndTimeInput.value = '17:00';
        newEventColorInput.value = '';
        
        eventTypeAllDay.checked = true; // メインのイベントタイプも初期に戻す
        toggleTimeInputs(); // メインの時間入力フィールドの表示を更新
        startTimeInput.value = '09:00';
        endTimeInput.value = '17:00';
        eventColorInput.value = '';
        selectSavedEventName.value = ''; // ドロップダウンをリセット
        updateDayGrid();
        messageArea.textContent = '';
        messageArea.classList.add('hidden');
      });
      
      // 日付グリッドを表示する関数
      function updateDayGrid() {
        dayGridDiv.innerHTML = '';
        selectedDays.clear();
        
        const [year, month] = monthInput.value.split('-').map(Number);
        if (isNaN(year) || isNaN(month)) {
          dayGridDiv.innerHTML = '<p class="text-red-500 text-center col-span-7">有効な年月を入力してください。</p>';
          return;
        }
        
        const daysInMonth = new Date(year, month, 0).getDate();
        const dayNames = ['日', '月', '火', '水', '木', '金', '土'];
        dayNames.forEach(name => {
          const dayNameDiv = document.createElement('div');
          dayNameDiv.textContent = name;
          dayNameDiv.classList.add('text-center', 'font-bold', 'text-gray-600', 'py-1', 'mobile-friendly-text');
          dayGridDiv.appendChild(dayNameDiv);
        });
        
        const firstDayOfMonth = new Date(year, month - 1, 1).getDay();
        
        for (let i = 0; i < firstDayOfMonth; i++) {
          const emptyDiv = document.createElement('div');
          dayGridDiv.appendChild(emptyDiv);
        }
        
        for (let i = 1; i <= daysInMonth; i++) {
          const button = document.createElement('button');
          button.textContent = i;
          button.classList.add('p-2', 'bg-gray-50', 'text-gray-800', 'rounded-lg', 'bg-white', 'border', 'border-gray-200', 'text-gray-800', 'hover:bg-gray-100', 'text-sm', 'font-medium', 'mobile-friendly-button');
          button.dataset.day = i;
          button.addEventListener('click', function() {
            this.classList.toggle('selected');
            const day = this.dataset.day;
            if (this.classList.contains('selected')) {
              selectedDays.add(day);
            } else {
              selectedDays.delete(day);
            }
          });
          dayGridDiv.appendChild(button);
        }
      }
      
      // メインのイベントタイプが時間指定の場合の制御
      function toggleTimeInputs() {
        if (eventTypeTimed.checked) {
          timeInputsDiv.style.display = 'grid';
        } else {
          timeInputsDiv.style.display = 'none';
        }
      }
      
      // 新しいイベント名追加用の時間入力フィールドの表示制御
      function toggleNewEventTimeInputs() {
        const newEventTimeInputsDiv = document.getElementById('newEventNameInputs');
        if (newEventEventTypeTimed.checked) {
          newEventTimeInputsDiv.style.display = 'grid';
        } else {
          newEventTimeInputsDiv.style.display = 'none';
        }
      }
    });
  </script>
</body>
</html>
```

メモ

**カレンダーグリッドの生成ロジック：**

```javascript
function updateDayGrid() {
  dayGridDiv.innerHTML = '';
  selectedDays.clear();
  
  const [year, month] = monthInput.value.split('-').map(Number);
  const daysInMonth = new Date(year, month, 0).getDate();
  const dayNames = ['日', '月', '火', '水', '木', '金', '土'];
  
  // 曜日ヘッダーを作成
  dayNames.forEach(name => {
    const dayNameDiv = document.createElement('div');
    dayNameDiv.textContent = name;
    dayNameDiv.classList.add('text-center', 'font-bold', 'text-gray-600', 'py-1');
    dayGridDiv.appendChild(dayNameDiv);
  });
  
  // 月の最初の日の曜日に合わせて空白を追加
  const firstDayOfMonth = new Date(year, month - 1, 1).getDay();
  for (let i = 0; i < firstDayOfMonth; i++) {
    const emptyDiv = document.createElement('div');
    dayGridDiv.appendChild(emptyDiv);
  }
  
  // 日付ボタンを生成
  for (let i = 1; i <= daysInMonth; i++) {
    const button = document.createElement('button');
    button.textContent = i;
    button.dataset.day = i;
    button.addEventListener('click', function() {
      this.classList.toggle('selected');
      const day = this.dataset.day;
      if (this.classList.contains('selected')) {
        selectedDays.add(day);
      } else {
        selectedDays.delete(day);
      }
    });
    dayGridDiv.appendChild(button);
  }
}
```

## セットアップ方法

### 1. Google Apps Scriptプロジェクトの作成

1. [Google Apps Script](https://script.google.com/) にアクセス
2. 「新しいプロジェクト」をクリック
3. プロジェクト名を「カレンダーイベント登録ツール」などに変更

### 2. コードの追加

1. `Code.gs` に上記のGASコードをコピー&ペースト
2. 「+」ボタンから「HTMLファイル」を選択
3. ファイル名を `Index` に設定
4. HTMLファイルに完全なHTMLコードを貼り付け

### 3. ウェブアプリとしてデプロイ

1. 「デプロイ」>「新しいデプロイ」をクリック
2. 種類：「ウェブアプリ」を選択
3. 実行ユーザー：「自分」
4. アクセスできるユーザー：「自分のみ」または「全員」
5. 「デプロイ」をクリック

### 4. 権限の承認

初回実行時にカレンダーへのアクセス権限を求められるので、承認してください。

## 使い方

### 基本的な使い方

1. **カレンダーIDの確認** - アプリを開くと自動的にあなたのメールアドレスが入力されます
2. **月の選択** - イベントを追加したい月を選択
3. **日付の選択** - カレンダーグリッドから複数の日付をクリックして選択
4. **イベント詳細の入力**
   - イベント名を入力
   - 終日または時間指定を選択
   - 時間指定の場合は開始・終了時間を設定
   - イベントの色を選択（任意）
5. **登録実行** - 「カレンダーにイベント登録」ボタンをクリック

### よく使うイベントの保存

1. 新しいイベント名セクションでイベント詳細を設定
2. 「イベント名を追加」ボタンで保存
3. 次回以降は保存済みイベント名から選択するだけで設定が自動入力

## 技術的なポイント

### PropertiesServiceによるデータ永続化

```javascript
var userProperties = PropertiesService.getUserProperties();
userProperties.setProperty('savedEventNames', JSON.stringify(eventNames));
```

ユーザー固有のイベント名リストを保存し、アプリを再利用した際に復元できます。

### 重複イベントの防止

```javascript
var events = calendar.getEvents(searchStartTime, searchEndTime, { search: eventTitle });
var eventExists = events.some(event => event.getTitle() === eventTitle);
```

同じ日付に同じ名前のイベントが既に存在する場合は、追加をスキップします。

### スマホ対応レスポンシブデザイン

```css
/* スマホ専用の最適化 */
@media (max-width: 640px) {
  .mobile-friendly-text { font-size: 18px !important; }
  .mobile-friendly-button { 
    font-size: 20px !important; 
    padding: 18px !important; 
    min-height: 56px; 
  }
  .day-grid button { 
    min-height: 56px; 
    min-width: 56px; 
  }
  input[type="radio"] { 
    width: 24px !important; 
    height: 24px !important; 
  }
}
```

**スマホでの使いやすさを徹底追求：**
- 文字サイズ18px以上で読みやすく
- ボタンの最小サイズ56px×56pxでタップしやすく
- iOS Safariの自動ズーム防止
- 縦配置レイアウトでスマホに最適化

### レスポンシブデザイン

TailwindCSSを使用し、デスクトップ・タブレット・スマホすべてで快適に使えるUIを実現しています。

## まとめ

このGASカレンダーイベント登録ツールを使うことで：

- **作業効率が大幅に向上** - 複数日付への一括登録が可能
- **ミスの削減** - 手動での繰り返し作業が不要
- **使い勝手の良さ** - 直感的なカレンダーUI
- **カスタマイズ性** - コードを修正して機能拡張可能

定期的なイベント登録作業がある方は、ぜひ試してみてください！

## 参考リンク

- [Google Apps Script公式ドキュメント](https://developers.google.com/apps-script)
- [Calendar Appリファレンス](https://developers.google.com/apps-script/reference/calendar)
- [TailwindCSS公式サイト](https://tailwindcss.com/)

---
